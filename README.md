# Init
## Why does this exist?
I hate setting up my environment every single time so here's a script to automate it

## Install
```
git clone git@github.com:BotPhil01/initBINIT.git initBINIT
cd initBINIT
sudo ./install.sh
```
